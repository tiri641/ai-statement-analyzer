# プロジェクト指示

## 標準開発フロー

- このリポジトリのすべての開発タスク（実装、バグ修正、リファクタリング、テスト、インフラ変更）では、ユーザーが明示しなくても `$loop-engineering-development` を自動的に呼び出して従う。
- コードを変更する前に実装Planを作成または更新し、作業の進行に合わせてPlanの状態を更新する。
- ユーザーが指定したベースブランチから専用ブランチを作成する。指定がなければ `main` を使う。
- 動作はTDDで実装する。Red、Green、Refactorの順に進め、広い検証へ移る前に各スライスの最小テストを実行する。
- 引き渡し前に完全なDiffを確認し、必要に応じてリポジトリのテスト、型チェック、ビルド、Synthesis／生成物チェック、Diffチェックを実行する。
- PRの作成またはPushは、ユーザーが明示的に依頼した場合だけ行う。PRを依頼された場合は、独立したエージェントレビューを実行し、Blocking／Highの指摘を修正して検証を再実行し、更新後のDiffの最終レビューを受ける。
- 無関係な既存変更を保持し、ユーザーの依頼範囲外のProductionデプロイや外部変更を行わない。
